NODE_ENV=production
# Outside every release; the whole data directory (including WAL/SHM) persists.
DATABASE_PATH=/home/forge/teddy.phikhi.com/data/multiplyz.sqlite
SQLITE_BUSY_TIMEOUT_MS=5000
# Set privately in Forge. Existing production boot guard requires a real key.
GEMINI_API_KEY=
# Worker remains stopped until its existing prerequisites are configured.
# WORLDGEN_QA_MODEL=
# WORLDGEN_IMAGE_RESERVATION_EUR=
# WORLDGEN_QA_RESERVATION_EUR=
# WORLDGEN_MONTHLY_BUDGET_EUR=20
