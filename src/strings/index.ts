import { fr } from "./fr";

/** Locale active (v1 = FR uniquement). */
export const LOCALE = "fr" as const;

/** Chaînes de l'application, depuis la table centralisée. */
export const strings = fr;

export type { Strings } from "./fr";
