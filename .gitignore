# Dependencies
node_modules/
.pnpm-store/

# Next.js
.next/
out/
next-env.d.ts
*.tsbuildinfo

# Production / build
build/
dist/

# Env & secrets
.env
.env.*
!.env.example

# SQLite (DB locale — jamais commitée)
*.sqlite
*.sqlite3
*.db
*.sqlite-wal
*.sqlite-shm
/data/

# Tests / coverage
coverage/
playwright-report/
test-results/
.playwright-mcp/

# Assets générés (IA) — sur disque VPS, pas dans le repo
/public/generated/
/storage/

# Photos sources de Teddy (Stage A) — input local, jamais commité (binaires lourds + perso)
/docs/teddy/

# Artefacts locaux
multiplyz-ds-*.png

# Claude Code (local)
.claude/settings.local.json

# Verrou de session pleine-durée (#298) — état runtime d'UN run, jamais partagé
.claude/skills/orchestrate/.session-lock.json

# OS / éditeur
.DS_Store
*.log
.idea/
.vscode/

# Outils owner-run non committés (génération assets, montage sign-off)
scripts/*.local.ts
